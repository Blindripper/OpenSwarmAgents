{
  "name": "openswarmagents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenSwarmAgents is a local-first decentralized coordination network for user-owned AI agents working on shared goals.",
  "scripts": {
    "dev": "node apps/server/src/server.mjs",
    "start": "node apps/server/src/server.mjs",
    "check": "node --check apps/server/src/server.mjs && node --check apps/web/public/theme-init.js && node --check apps/web/public/app.js && python3 -m py_compile apps/connector/connector.py",
    "check:rc": "npm run check && node scripts/rc-smoke.mjs",
    "db:schema": "node scripts/print-schema-path.mjs",
    "export:sql": "node scripts/export-json-to-sql.mjs"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=22"
  }
}
