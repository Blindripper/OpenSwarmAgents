# 2026-09-03

## Topbar Redesign & DID-Features
- Header umgebaut: FLOP Pledges raus, DID-Menü rein, nur 3 Metriken (DID, $FLOP)
- DidMenu-Komponente: klickbar/aufklappbar, zeigt Node-DID + alle Agent-DIDs kopierbar
- `agentDidForProfile()` generiert deterministische Ed25519 `did:key:z...` aus Node-ID + Profil-ID
- `publicAgentGuiProfile()` gibt jetzt `did`-Feld aus
- `agent.profile_path` in `AgentProfile`-Feld ergänzt

## Wallet-Selector-System
- Phantom Wallet (evmAsk.js) blockierte Login: hijackt `window.ethereum`, wirft "Unexpected error"
- Neue `wallet-provider.ts`: EIP-6963 Discovery, filtert Phantom (isPhantom), MetaMask bevorzugt
- `WalletSelectorModal.tsx`: Multi-Wallet-Picker bei mehreren installierten Wallets
- `selectWallet()` wirft "MULTIPLE_WALLETS"-Fehler → Modal öffnet sich
- Auto-select bei nur einer Wallet

## Systemd-Service
- `/etc/systemd/system/osa-dashboard.service` installiert und aktiv
- `scripts/install-node.sh` hat `--systemd` Flag
- `scripts/install-systemd-service.sh` separates Skript
- README für Install aktualisiert

## Frühere Session-Arbeit
- TDZ Bug in `getMetaMaskProvider()` gefixt (const fallbackTimer vor handler)
- Technocore DID-Profile auf Technocore registriert (`ensureTechnocoreDidProfile`)
- Neue Agents bekommen jetzt automatisch DIDs (via `agentDidForProfile` + $store update)
- `.env.example` mit Technocore-Konfig dokumentiert
# 2026-09-03

## Topbar Redesign & DID-Features
- Header umgebaut: FLOP Pledges raus, DID-Menü rein, nur 3 Metriken (DID, $FLOP)
- DidMenu-Komponente: klickbar/aufklappbar, zeigt Node-DID + alle Agent-DIDs kopierbar
- `agentDidForProfile()` generiert deterministische Ed25519 `did:key:z...` aus Node-ID + Profil-ID
- `publicAgentGuiProfile()` gibt jetzt `did`-Feld aus

## Wallet-Selector-System
- Phantom Wallet (evmAsk.js) blockierte Login: hijackt `window.ethereum`, wirft "Unexpected error"
- Neue `wallet-provider.ts`: EIP-6963 Discovery, filtert Phantom (isPhantom), MetaMask bevorzugt
- `WalletSelectorModal.tsx`: Multi-Wallet-Picker bei mehreren installierten Wallets
- `selectWallet()` wirft "MULTIPLE_WALLETS"-Fehler → Modal öffnet sich
- Auto-select bei nur einer Wallet

## Systemd-Service
- `/etc/systemd/system/osa-dashboard.service` installiert und aktiv
- `scripts/install-node.sh` hat `--systemd` Flag
- `scripts/install-systemd-service.sh` separates Skript
- README für Install aktualisiert

## In progress: Work-Flow mit Agent-Selection, Workspace-Room pro Claim, Submit-Result automatisch
- User will: "Work" Tab → Claim → Agent auswählen → Room in Workspaces/Projects → Job ausführen → Automatisch "submit result"
- "Latest Projects" und "Project Discovery" entfernen
- README unterhaltsam aktualisieren
- Alles auf GitHub pushen
# 2026-09-03 (continued)

## Work-Flow mit Agent-Selection, Room-Creation, Auto-Submit
- `POST /api/jobs/claim` akzeptiert jetzt `agent_id` → erstellt einen neuen Workspace-Room mit Desk + Session
- Claimed Jobs erscheinen als Room in "Workspaces / Projects"
- Auto-Submit beim Session-Completion via SSE/Event-Callback
- "Project Discovery" und "Latest Projects" Tabs entfernt
- README auf unterhaltsam + nicht-technisch umgeschrieben
- Technische Details in separate Dokumentation ausgelagert
- Alles auf `origin/main` gepusht
## Work-Flow mit Agent-Selection (in progress)
- User will: "Work" Tab → Claim → Agent auswählen → Room in Workspaces/Projects → Job ausführen → Automatisch "submit result"
- "Latest Projects" und "Project Discovery" entfernen
- README unterhaltsam aktualisieren, technische Details in separates Doc
- Alles auf GitHub pushen
- Status: Implementation gestartet, Server-Routen analysiert (POST /api/jobs/claim, Room-Erstellung, Desk/Session-Logik) <!-- project: github.com/Blindripper/OpenSwarmAgents -->

## Topbar & DIDs
- DidMenu: Node-DID + alle Agent-DIDs (echte `did:key:z...`), kopierbar, aufklappbar
- `publicAgentGuiProfile()` gibt `did`-Feld aus <!-- project: github.com/Blindripper/OpenSwarmAgents -->

## Wallet-Selector-System
- wallet-provider.ts: EIP-6963 Discovery, Phantom-Filter, MetaMask bevorzugt
- WalletSelectorModal.tsx: Multi-Wallet-Picker <!-- project: github.com/Blindripper/OpenSwarmAgents -->

## Systemd-Service
- /etc/systemd/system/osa-dashboard.service
- install-node.sh --systemd, install-systemd-service.sh <!-- project: github.com/Blindripper/OpenSwarmAgents -->
## Work-Flow mit Agent-Selection (in progress)
- User will: "Work" Tab → Claim → Agent auswählen → Room in Workspaces/Projects → Job ausführen → Automatisch "submit result" <!-- project: github.com/Blindripper/OpenSwarmAgents -->
- "Latest Projects" und "Project Discovery" entfernen <!-- project: github.com/Blindripper/OpenSwarmAgents -->
- README unterhaltsam aktualisieren, technische Details in separates Doc <!-- project: github.com/Blindripper/OpenSwarmAgents -->
- Alles auf GitHub pushen <!-- project: github.com/Blindripper/OpenSwarmAgents -->
- Status: Server-Routen analysiert, POST /api/jobs/claim + Room-Erstellungs-Logik identifiziert, Implementation läuft <!-- project: github.com/Blindripper/OpenSwarmAgents -->

## Topbar & DIDs
- DidMenu: Node-DID + alle Agent-DIDs (echte `did:key:z...`), kopierbar, aufklappbar <!-- project: github.com/Blindripper/OpenSwarmAgents -->
- `publicAgentGuiProfile()` gibt `did`-Feld aus <!-- project: github.com/Blindripper/OpenSwarmAgents -->

## Wallet-Selector-System
- wallet-provider.ts: EIP-6963 Discovery, Phantom-Filter, MetaMask bevorzugt <!-- project: github.com/Blindripper/OpenSwarmAgents -->
- WalletSelectorModal.tsx: Multi-Wallet-Picker <!-- project: github.com/Blindripper/OpenSwarmAgents -->

## Systemd-Service
- /etc/systemd/system/osa-dashboard.service <!-- project: github.com/Blindripper/OpenSwarmAgents -->
- install-node.sh --systemd, install-systemd-service.sh <!-- project: github.com/Blindripper/OpenSwarmAgents -->